---
phase: 07-operations-api-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/thinktank/admin/__init__.py
  - src/thinktank/admin/main.py
  - src/thinktank/admin/dependencies.py
  - src/thinktank/admin/routers/__init__.py
  - src/thinktank/admin/routers/dashboard.py
  - src/thinktank/admin/routers/llm_panel.py
  - src/thinktank/admin/routers/categories.py
  - src/thinktank/admin/templates/base.html
  - src/thinktank/admin/templates/dashboard.html
  - src/thinktank/admin/templates/llm_panel.html
  - src/thinktank/admin/templates/categories.html
  - src/thinktank/admin/templates/partials/queue_depth.html
  - src/thinktank/admin/templates/partials/error_log.html
  - src/thinktank/admin/templates/partials/source_health.html
  - src/thinktank/admin/templates/partials/gpu_status.html
  - src/thinktank/admin/templates/partials/rate_limits.html
  - src/thinktank/admin/templates/partials/cost_tracker.html
  - src/thinktank/admin/templates/partials/llm_status.html
  - src/thinktank/admin/templates/partials/llm_pending.html
  - src/thinktank/admin/templates/partials/llm_recent.html
  - src/thinktank/admin/templates/partials/category_tree.html
  - pyproject.toml
  - tests/integration/test_admin_dashboard.py
  - tests/integration/test_admin_llm_panel.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-04
  - OPS-05

must_haves:
  truths:
    - "Admin dashboard displays live queue depth grouped by job type and status"
    - "Admin dashboard shows recent error log with error category and job type"
    - "Admin dashboard shows source health (active vs errored sources)"
    - "Admin dashboard shows GPU scaling status (up/down, last scale event)"
    - "Admin dashboard shows rate limit gauges (current usage vs configured limits per API)"
    - "Admin dashboard shows API cost tracker (estimated USD from api_usage)"
    - "Dashboard widgets auto-refresh every 10 seconds via HTMX"
    - "LLM decision panel shows pending approvals with time-since-created"
    - "LLM decision panel shows recent decisions with reasoning"
    - "LLM decision panel allows human override with admin username and reasoning logged to llm_reviews"
    - "Timed-out LLM reviews (older than llm_timeout_hours) are highlighted"
    - "Category taxonomy is viewable and editable in admin dashboard"
  artifacts:
    - path: "src/thinktank/admin/main.py"
      provides: "Separate FastAPI admin application"
      contains: "FastAPI"
    - path: "src/thinktank/admin/routers/dashboard.py"
      provides: "Dashboard page and HTMX partials"
      exports: ["router"]
    - path: "src/thinktank/admin/routers/llm_panel.py"
      provides: "LLM decision panel with override"
      exports: ["router"]
    - path: "src/thinktank/admin/routers/categories.py"
      provides: "Category taxonomy management"
      exports: ["router"]
    - path: "src/thinktank/admin/templates/base.html"
      provides: "Base HTML layout with HTMX CDN and nav"
      contains: "htmx.org"
    - path: "src/thinktank/admin/templates/dashboard.html"
      provides: "Dashboard page with auto-refresh widgets"
      contains: "hx-trigger"
  key_links:
    - from: "src/thinktank/admin/main.py"
      to: "src/thinktank/admin/routers/*.py"
      via: "app.include_router()"
      pattern: "include_router"
    - from: "src/thinktank/admin/routers/dashboard.py"
      to: "src/thinktank/models/job.py"
      via: "SQLAlchemy GROUP BY query for queue depth"
      pattern: "GROUP BY.*job_type"
    - from: "src/thinktank/admin/routers/llm_panel.py"
      to: "src/thinktank/models/review.py"
      via: "LLMReview query and override update"
      pattern: "LLMReview"
    - from: "src/thinktank/admin/templates/dashboard.html"
      to: "src/thinktank/admin/routers/dashboard.py"
      via: "HTMX hx-get for partial endpoints"
      pattern: "hx-get.*partials"
    - from: "src/thinktank/admin/dependencies.py"
      to: "src/thinktank/database.py"
      via: "Shared engine import for admin session"
      pattern: "async_session_factory"
---

<objective>
Build the HTMX-powered admin dashboard as a separate FastAPI application -- providing human oversight of the pipeline with live queue depth, error logs, source health, GPU status, rate limit gauges, API cost tracking, LLM decision panel with human override, and category taxonomy management.

Purpose: Enable human monitoring and intervention for the autonomous pipeline. The dashboard is the primary operational interface for the system administrator.
Output: Complete admin FastAPI app with Jinja2 templates, HTMX auto-refresh, LLM override panel, and category management.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-operations-api-polish/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/thinktank/database.py:
```python
engine = create_engine_from_url(settings.database_url)
async_session_factory = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

From src/thinktank/api/dependencies.py (pattern to replicate for admin):
```python
async def get_session() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        yield session
```

From src/thinktank/models/job.py:
```python
class Job(Base):
    __tablename__ = "jobs"
    job_type: Mapped[str]
    status: Mapped[str]
    priority: Mapped[int]
    error: Mapped[Optional[str]]
    error_category: Mapped[Optional[str]]
    created_at: Mapped[datetime]
```

From src/thinktank/models/review.py:
```python
class LLMReview(Base):
    __tablename__ = "llm_reviews"
    id: Mapped[uuid_pk]
    review_type: Mapped[str]
    decision: Mapped[Optional[str]]
    decision_reasoning: Mapped[Optional[str]]
    overridden_by: Mapped[Optional[str]]
    overridden_at: Mapped[Optional[datetime]]
    override_reasoning: Mapped[Optional[str]]
    tokens_used: Mapped[Optional[int]]
    created_at: Mapped[datetime]
```

From src/thinktank/models/source.py:
```python
class Source(Base):
    __tablename__ = "sources"
    approval_status: Mapped[str]
    active: Mapped[bool]
    error_count: Mapped[int]
    last_fetched: Mapped[Optional[datetime]]
```

From src/thinktank/models/config_table.py:
```python
class SystemConfig(Base):
    __tablename__ = "system_config"
    key: Mapped[str]  # TEXT PK
    value: Mapped[dict]  # JSONB
```

From src/thinktank/models/category.py:
```python
class Category(TimestampMixin, Base):
    __tablename__ = "categories"
    id: Mapped[uuid_pk]
    slug: Mapped[str]  # unique
    name: Mapped[str]
    parent_id: Mapped[Optional[uuid.UUID]]  # FK categories.id
    description: Mapped[str]
    parent: Mapped[Optional["Category"]]  # lazy="selectin"
    children: Mapped[list["Category"]]  # lazy="selectin"
```

From src/thinktank/models/api_usage.py:
```python
class ApiUsage(Base):
    __tablename__ = "api_usage"
    api_name: Mapped[str]
    period_start: Mapped[datetime]
    call_count: Mapped[int]
    estimated_cost_usd: Mapped[Optional[float]]
```

From src/thinktank/models/rate_limit.py:
```python
class RateLimitUsage(Base):
    __tablename__ = "rate_limit_usage"
    api_name: Mapped[str]
    called_at: Mapped[datetime]
```

From docker/Dockerfile.admin (already exists):
The admin app is expected at `src.thinktank.admin.main:app`

From src/thinktank/config.py:
```python
class Settings(BaseSettings):
    service_name: str = "thinktank-api"
    # Admin app can use same Settings with different service_name
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin FastAPI app, dashboard with HTMX partials, and integration tests</name>
  <files>
    src/thinktank/admin/__init__.py,
    src/thinktank/admin/main.py,
    src/thinktank/admin/dependencies.py,
    src/thinktank/admin/routers/__init__.py,
    src/thinktank/admin/routers/dashboard.py,
    src/thinktank/admin/templates/base.html,
    src/thinktank/admin/templates/dashboard.html,
    src/thinktank/admin/templates/partials/queue_depth.html,
    src/thinktank/admin/templates/partials/error_log.html,
    src/thinktank/admin/templates/partials/source_health.html,
    src/thinktank/admin/templates/partials/gpu_status.html,
    src/thinktank/admin/templates/partials/rate_limits.html,
    src/thinktank/admin/templates/partials/cost_tracker.html,
    pyproject.toml,
    tests/integration/test_admin_dashboard.py
  </files>
  <action>
    1. Add dependencies to `pyproject.toml`: `uv add jinja2 python-multipart`

    2. Create `src/thinktank/admin/__init__.py` (empty).

    3. Create `src/thinktank/admin/dependencies.py`:
       - Import `async_session_factory` from `thinktank.database` (reuse existing engine/pool)
       - Define `get_session()` async generator matching `api/dependencies.py` pattern exactly
       - Define `get_templates()` returning `Jinja2Templates(directory=Path(__file__).parent / "templates")`

    4. Create `src/thinktank/admin/main.py`:
       - Separate FastAPI app: `app = FastAPI(title="ThinkTank Admin", ...)`
       - Use same lifespan pattern as api/main.py (verify DB, configure logging with service_name="thinktank-admin")
       - Add CorrelationIDMiddleware
       - Include dashboard_router, llm_panel_router, categories_router
       - Mount static files if needed (or rely on CDN for HTMX)
       - DO NOT import from thinktank.api.main -- admin is fully separate

    5. Create `src/thinktank/admin/routers/__init__.py` (empty).

    6. Create `src/thinktank/admin/routers/dashboard.py`:
       - `router = APIRouter(prefix="/admin")`
       - GET "/" -> Full dashboard page (renders `dashboard.html`)
       - GET "/partials/queue-depth" -> HTML fragment with job counts by type and status. Query: `SELECT job_type, status, COUNT(*) FROM jobs GROUP BY job_type, status` via `text()`
       - GET "/partials/error-log" -> HTML fragment with last 20 failed jobs. Query: `SELECT job_type, error, error_category, created_at FROM jobs WHERE status = 'failed' ORDER BY created_at DESC LIMIT 20`
       - GET "/partials/source-health" -> HTML fragment with source counts by approval_status. Query on sources table: total, approved, errored (error_count > 0), inactive
       - GET "/partials/gpu-status" -> HTML fragment with GPU scaling status. Query system_config for `gpu_service_status` key (or show "Unknown" if not set). Show last scale-up/down time from system_config
       - GET "/partials/rate-limits" -> HTML fragment with rate limit gauges. For each API in (listennotes, youtube, podcastindex, anthropic), query `SELECT COUNT(*) FROM rate_limit_usage WHERE api_name = :api AND called_at > LOCALTIMESTAMP - INTERVAL '1 hour'`. Compare against configured limits from system_config (default: listennotes=100, youtube=200, podcastindex=500, anthropic=50)
       - GET "/partials/cost-tracker" -> HTML fragment with API costs. Query: `SELECT api_name, SUM(call_count), SUM(estimated_cost_usd) FROM api_usage WHERE period_start > LOCALTIMESTAMP - INTERVAL '24 hours' GROUP BY api_name`
       - All partial endpoints use `templates.TemplateResponse("partials/xxx.html", {"request": request, ...})`

    7. Create Jinja2 templates:
       - `base.html`: DOCTYPE, head with HTMX CDN (`https://unpkg.com/htmx.org@2.0.4`), minimal CSS (dashboard-grid, card, gauge styles from research), nav with links to Dashboard, LLM Panel, Categories. No JavaScript framework.
       - `dashboard.html`: Extends base.html. Contains 6 widget divs each with `hx-get="/admin/partials/xxx" hx-trigger="load, every 10s" hx-swap="innerHTML"` and a "Loading..." placeholder.
       - `partials/queue_depth.html`: Table with job_type, pending count, running count, failed count, completed count
       - `partials/error_log.html`: Table with timestamp, job_type, error_category, error (truncated to 100 chars)
       - `partials/source_health.html`: Summary stats: total sources, approved, with errors, inactive
       - `partials/gpu_status.html`: Status indicator (up/down/unknown), last scale event time
       - `partials/rate_limits.html`: For each API: gauge bar showing current_usage/limit with color coding (green <50%, yellow 50-80%, red >80%)
       - `partials/cost_tracker.html`: Table with api_name, call count (24h), estimated cost (24h)

    8. Write `tests/integration/test_admin_dashboard.py`:
       - Create admin test client fixture using ASGITransport with admin app (import `from thinktank.admin.main import app as admin_app`)
       - Test: GET /admin/ returns 200 with HTML containing "ThinkTank Admin"
       - Test: GET /admin/partials/queue-depth returns 200 with HTML
       - Test: GET /admin/partials/error-log returns 200 with HTML
       - Test: GET /admin/partials/source-health returns 200 with HTML
       - Test: GET /admin/partials/gpu-status returns 200 with HTML
       - Test: GET /admin/partials/rate-limits returns 200 with HTML
       - Test: GET /admin/partials/cost-tracker returns 200 with HTML
       - Test: Seed jobs with various statuses, verify queue-depth partial contains correct counts
       - Test: Seed failed job, verify error-log partial contains the error text
       - Use `pytestmark = pytest.mark.anyio` and existing factories

    IMPORTANT conventions:
    - Admin app imports from `thinktank.database`, `thinktank.config` (not src.thinktank) for app-level imports matching api/main.py pattern
    - Router handlers import models from `thinktank.models.*` for consistency
    - Use `text()` SQL for aggregate queries (matching existing handler patterns)
    - Partial templates are standalone HTML fragments -- NO `{% extends "base.html" %}`
    - Full pages DO extend base.html
    - Timezone-naive datetimes throughout
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/luna/ThinkTank && uv run pytest tests/integration/test_admin_dashboard.py -x -v</automated>
  </verify>
  <done>Admin dashboard serves at /admin/ with 6 auto-refreshing HTMX widgets (queue depth, error log, source health, GPU status, rate limits, cost tracker). All partial endpoints return valid HTML fragments. Integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: LLM decision panel with human override, category management, and integration tests</name>
  <files>
    src/thinktank/admin/routers/llm_panel.py,
    src/thinktank/admin/routers/categories.py,
    src/thinktank/admin/templates/llm_panel.html,
    src/thinktank/admin/templates/partials/llm_pending.html,
    src/thinktank/admin/templates/partials/llm_recent.html,
    src/thinktank/admin/templates/partials/llm_status.html,
    src/thinktank/admin/templates/categories.html,
    src/thinktank/admin/templates/partials/category_tree.html,
    src/thinktank/admin/main.py,
    tests/integration/test_admin_llm_panel.py
  </files>
  <action>
    1. Create `src/thinktank/admin/routers/llm_panel.py`:
       - `router = APIRouter(prefix="/admin/llm", tags=["llm"])`
       - GET "/" -> Full LLM panel page (renders `llm_panel.html`). Query for LLM-related system_config values (llm_timeout_hours).
       - GET "/partials/pending" -> HTML fragment with pending LLM reviews. Query: `SELECT * FROM llm_reviews WHERE decision IS NULL ORDER BY created_at ASC`. For each, compute time_since_created. Highlight rows where `now - created_at > llm_timeout_hours` with `.timeout-highlight` CSS class. Show review_type, target info from context_snapshot, created_at, time waiting.
       - GET "/partials/recent" -> HTML fragment with last 20 completed reviews. Query: `SELECT * FROM llm_reviews WHERE decision IS NOT NULL ORDER BY created_at DESC LIMIT 20`. Show review_type, decision, reasoning (truncated), tokens_used, overridden status.
       - GET "/partials/status" -> HTML fragment showing LLM system status. Query pending count, avg review time, total tokens used (last 24h), override count.
       - POST "/override/{review_id}" -> Process human override. Accept Form fields: `override_decision` (approve/reject), `override_reasoning` (text), `admin_username` (text, default "admin"). Update LLMReview: set `overridden_by`, `overridden_at` (using `datetime.now(UTC).replace(tzinfo=None)`), `override_reasoning`, and `decision` = override_decision. Then apply the override to the target entity:
         - If review_type == "thinker_approval": update Thinker.approval_status to the override_decision
         - If review_type == "source_approval": update Source.approval_status
         - If review_type == "candidate_review": update CandidateThinker.status
         - Use context_snapshot to find the target entity ID (e.g., context_snapshot["thinker_id"])
         - Return HTMX swap: re-render the pending partial to remove the overridden item
       - POST requires `python-multipart` (already added in Task 1)

    2. Create `src/thinktank/admin/routers/categories.py`:
       - `router = APIRouter(prefix="/admin/categories", tags=["categories"])`
       - GET "/" -> Full category management page (renders `categories.html`)
       - GET "/partials/tree" -> HTML fragment showing category tree. Query all categories with parent_id = NULL as roots, then render hierarchically using children relationship. Show slug, name, description, child count.
       - POST "/create" -> Create new category via Form (name, slug, description, parent_id optional). Insert using ORM. Return re-rendered tree partial.
       - POST "/update/{category_id}" -> Update category name/description via Form. Return re-rendered tree partial.
       - POST "/delete/{category_id}" -> Delete category (only if no children and no thinker_categories referencing it). Return re-rendered tree partial or error message.

    3. Create Jinja2 templates:
       - `llm_panel.html`: Extends base.html. Three sections: LLM Status (auto-refresh), Pending Approvals (auto-refresh), Recent Decisions (auto-refresh). Each with `hx-get hx-trigger="load, every 10s"`. Override form per pending review with action="/admin/llm/override/{id}" method="post" fields for decision (select: approve/reject), reasoning (textarea), admin_username (text input, default "admin"). Form uses `hx-post` and `hx-target` for HTMX submission.
       - `partials/llm_pending.html`: Table/list of pending reviews. Each row has override form. Rows with timeout get `.timeout-highlight` class.
       - `partials/llm_recent.html`: Table of recent decisions with decision, reasoning (expandable), tokens, override indicator.
       - `partials/llm_status.html`: Stats: pending count, avg tokens/review, total cost estimate (24h), override count.
       - `categories.html`: Extends base.html. Tree view section (auto-refresh) + create form at bottom.
       - `partials/category_tree.html`: Nested list rendering categories hierarchically. Each node shows name, slug, expand/collapse for children, edit/delete buttons. Uses recursive Jinja2 macro for nesting.

    4. Update `src/thinktank/admin/main.py` to include llm_panel_router and categories_router (if not already included in Task 1 -- they should be included but the routers files don't exist until this task creates them, so add the imports and include_router calls).

    5. Write `tests/integration/test_admin_llm_panel.py`:
       - Create admin test client fixture (same pattern as dashboard tests)
       - Test: GET /admin/llm/ returns 200 with HTML containing "LLM"
       - Test: GET /admin/llm/partials/pending returns 200
       - Test: GET /admin/llm/partials/recent returns 200
       - Test: Seed LLMReview with decision=None, verify it appears in pending partial
       - Test: Seed LLMReview with decision="approve", verify it appears in recent partial
       - Test: Seed LLMReview created 3+ hours ago (simulating timeout), verify it has timeout-highlight class when llm_timeout_hours=2 config is set
       - Test: POST /admin/llm/override/{id} with decision=approve updates the LLMReview record (overridden_by, override_reasoning, decision set)
       - Test: POST override for thinker_approval review updates the associated Thinker.approval_status
       - Test: GET /admin/categories/ returns 200
       - Test: POST /admin/categories/create with name/slug/description creates a category
       - Use `pytestmark = pytest.mark.anyio`
       - Use factories: `create_llm_review`, `create_thinker`, `create_source`, `create_system_config`, `create_category`

    IMPORTANT conventions:
    - Override applies the decision to the target entity (thinker/source/candidate) in the same transaction
    - Use Form(...) from fastapi for POST data (not JSON body) since this is HTML form submission
    - Override response uses HTMX: either return re-rendered partial or use HX-Redirect header
    - Timeout detection: read llm_timeout_hours from system_config (default 2 if not set). Compare against review created_at
    - Category delete should return 400 if category has children or thinker associations
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/luna/ThinkTank && uv run pytest tests/integration/test_admin_llm_panel.py -x -v</automated>
  </verify>
  <done>LLM decision panel shows pending reviews (highlighted when timed out), recent decisions, and supports human override with full audit trail. Category taxonomy is viewable, creatable, editable, and deletable. Integration tests pass for all LLM panel and category management operations.</done>
</task>

</tasks>

<verification>
1. All admin integration tests pass: `uv run pytest tests/integration/test_admin_dashboard.py tests/integration/test_admin_llm_panel.py -x -v`
2. Full existing test suite still passes: `uv run pytest tests/ -x --timeout=60`
3. Admin app starts independently from API app
4. Dashboard widgets use HTMX auto-refresh at 10s intervals
5. LLM override creates proper audit trail in llm_reviews
</verification>

<success_criteria>
- Admin dashboard at /admin/ displays 6 live-updating widgets
- HTMX provides 10-second auto-refresh without JavaScript framework
- LLM panel shows pending/recent decisions with timeout highlighting
- Human override updates both llm_reviews and target entity in one transaction
- Category taxonomy management supports CRUD with hierarchical display
- All 582+ existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-operations-api-polish/07-02-SUMMARY.md`
</output>
