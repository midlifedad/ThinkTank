---
phase: 03-content-ingestion-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/rss/podcast_basic.xml
  - tests/fixtures/rss/podcast_itunes.xml
  - tests/fixtures/rss/podcast_no_duration.xml
  - tests/fixtures/rss/podcast_duplicates.xml
  - tests/fixtures/rss/podcast_short_episodes.xml
  - tests/fixtures/rss/podcast_skip_titles.xml
  - alembic/versions/xxxx_add_pg_trgm_extension.py
  - tests/conftest.py
autonomous: true
requirements:
  - DISC-04

must_haves:
  truths:
    - "pg_trgm extension is available for trigram similarity queries"
    - "RSS fixture files provide deterministic test data for all integration test scenarios"
    - "Test database has pg_trgm extension enabled and GiST index created"
  artifacts:
    - path: "tests/fixtures/rss/"
      provides: "Checked-in RSS fixture files for deterministic testing"
    - path: "alembic/versions/xxxx_add_pg_trgm_extension.py"
      provides: "Alembic migration enabling pg_trgm and creating GiST index"
    - path: "tests/conftest.py"
      provides: "Updated test DB setup with pg_trgm extension"
  key_links:
    - from: "tests/conftest.py"
      to: "pg_trgm extension"
      via: "CREATE EXTENSION IF NOT EXISTS pg_trgm before create_all"
      pattern: "CREATE EXTENSION IF NOT EXISTS pg_trgm"
---

<objective>
Create RSS fixture files for deterministic integration testing, add the pg_trgm Alembic migration, and update the test conftest to enable pg_trgm in the test database.

Purpose: RSS fixtures provide the deterministic feed data that all integration tests in Plans 03 and 04 rely on. The pg_trgm migration enables trigram similarity for candidate dedup. The conftest update ensures the test database matches production capabilities.

Output: 6 RSS fixture XML files in `tests/fixtures/rss/`, 1 Alembic migration, updated `tests/conftest.py`.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-ingestion-pipeline/03-RESEARCH.md
@STANDARDS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RSS fixture files, pg_trgm Alembic migration, and update test conftest</name>
  <files>
    tests/fixtures/rss/podcast_basic.xml,
    tests/fixtures/rss/podcast_itunes.xml,
    tests/fixtures/rss/podcast_no_duration.xml,
    tests/fixtures/rss/podcast_duplicates.xml,
    tests/fixtures/rss/podcast_short_episodes.xml,
    tests/fixtures/rss/podcast_skip_titles.xml,
    alembic/versions/xxxx_add_pg_trgm_extension.py,
    tests/conftest.py
  </files>
  <action>
    1. Create `tests/fixtures/rss/` directory.

    2. Create 6 RSS fixture XML files per RESEARCH.md Pattern 3:

    `podcast_basic.xml` -- Standard podcast RSS feed with 3 episodes. Each has title, pubDate, enclosure (with url, length, type="audio/mpeg"), link, description. Use realistic podcast episode names (e.g., "Deep Dive into AI Safety", "The Future of Energy", "Crypto Markets Q4 Review"). Durations: 3600, 2400, 5400 seconds.

    `podcast_itunes.xml` -- Feed with iTunes namespace extensions. Include `<itunes:duration>01:30:00</itunes:duration>`, `<itunes:author>Test Host</itunes:author>`, `<itunes:owner>` element. 2 episodes with itunes:duration in different formats (HH:MM:SS and MM:SS).

    `podcast_no_duration.xml` -- Feed with 2 episodes where neither has a duration field (no itunes:duration, no enclosure length). Tests graceful handling of missing duration.

    `podcast_duplicates.xml` -- Feed with 2 episodes that have different URLs but same title, date, and duration (to test fingerprint dedup). Also include one episode with a URL that differs only in tracking params (to test URL normalization dedup).

    `podcast_short_episodes.xml` -- Feed with 3 episodes: one at 120s ("Quick Update"), one at 300s ("Short Bonus"), one at 3600s ("Full Episode"). Tests duration filtering.

    `podcast_skip_titles.xml` -- Feed with 4 episodes: "Season 3 Trailer", "Best of 2025", "Full Interview with Expert", "Announcement: New Season". Tests title pattern filtering (first two and last should be skipped with default patterns).

    All fixture files must be valid RSS 2.0 XML with proper XML declaration and channel wrapper.

    3. Create Alembic migration for pg_trgm:
    Run `uv run alembic revision --autogenerate -m "add_pg_trgm_extension"` to get the migration template, then edit it:
    - upgrade(): `op.execute("CREATE EXTENSION IF NOT EXISTS pg_trgm")`
    - upgrade(): Create GiST index on candidate_thinkers.normalized_name: `op.create_index("ix_candidate_thinkers_trgm", "candidate_thinkers", [sa.text("normalized_name gist_trgm_ops")], postgresql_using="gist")`
    - downgrade(): `op.drop_index("ix_candidate_thinkers_trgm")` then `op.execute("DROP EXTENSION IF EXISTS pg_trgm")`

    4. Update `tests/conftest.py` engine fixture to include `CREATE EXTENSION IF NOT EXISTS pg_trgm` before `Base.metadata.create_all()`. This ensures the test database has pg_trgm available for integration tests in Plans 03/04. Add it right after the `drop_all` call:
    ```python
    async with eng.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)
        await conn.execute(text("CREATE EXTENSION IF NOT EXISTS pg_trgm"))
        await conn.run_sync(Base.metadata.create_all)
    ```

    Also add the GiST index creation after `create_all` (since SQLAlchemy create_all does not run Alembic migrations):
    ```python
    await conn.execute(text(
        "CREATE INDEX IF NOT EXISTS ix_candidate_thinkers_trgm "
        "ON candidate_thinkers USING gist (normalized_name gist_trgm_ops)"
    ))
    ```
  </action>
  <verify>
    <automated>uv run alembic upgrade head && python -c "import xml.etree.ElementTree as ET; [ET.parse(f'tests/fixtures/rss/{f}') for f in ['podcast_basic.xml','podcast_itunes.xml','podcast_no_duration.xml','podcast_duplicates.xml','podcast_short_episodes.xml','podcast_skip_titles.xml']]; print('All fixtures valid XML')"</automated>
  </verify>
  <done>
    6 RSS fixture files exist in tests/fixtures/rss/ and are valid XML. Alembic migration for pg_trgm runs successfully. tests/conftest.py creates pg_trgm extension and GiST index in test database.
  </done>
</task>

</tasks>

<verification>
- Alembic migration applies cleanly: `uv run alembic upgrade head`
- All 6 RSS fixture files are valid XML
- All existing Phase 1/2 tests still pass: `uv run pytest tests/ -x -q`
</verification>

<success_criteria>
1. pg_trgm Alembic migration applies to fresh database
2. 6 RSS fixture files exist and are valid XML
3. tests/conftest.py enables pg_trgm in test database
4. Existing test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-ingestion-pipeline/03-02-SUMMARY.md`
</output>
