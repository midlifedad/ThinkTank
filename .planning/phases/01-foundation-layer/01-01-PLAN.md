---
phase: 01-foundation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/thinktank/__init__.py
  - src/thinktank/database.py
  - src/thinktank/api/__init__.py
  - src/thinktank/api/main.py
  - src/thinktank/api/health.py
  - src/thinktank/api/dependencies.py
  - src/thinktank/worker/__init__.py
  - docker-compose.yml
  - docker-compose.test.yml
  - .env.example
  - .gitignore
  - .pre-commit-config.yaml
  - .python-version
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/integration/__init__.py
  - tests/integration/test_health.py
autonomous: true
requirements:
  - FNDTN-07
  - FNDTN-08

must_haves:
  truths:
    - "uv sync installs all dependencies without error"
    - "FastAPI application starts with async lifespan connecting to PostgreSQL"
    - "GET /health returns 200 with {status: healthy} when DB is connected"
    - "GET /health returns 503 when DB is unavailable"
    - "ruff check src/ and mypy src/ pass with zero errors"
    - "pre-commit hooks are configured and runnable"
    - "Docker Compose starts PostgreSQL 16 for both dev and test"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project configuration with all dependencies, ruff, mypy, pytest settings"
      contains: "thinktank"
    - path: "src/thinktank/api/main.py"
      provides: "FastAPI app with async lifespan and CORS"
      exports: ["app"]
    - path: "src/thinktank/api/health.py"
      provides: "Health check endpoint"
      contains: "GET /health"
    - path: "src/thinktank/database.py"
      provides: "Async engine and session factory"
      exports: ["engine", "async_session_factory", "get_session"]
    - path: "docker-compose.yml"
      provides: "Local dev PostgreSQL 16"
      contains: "postgres:16-alpine"
    - path: "docker-compose.test.yml"
      provides: "Test PostgreSQL 16 on port 5433"
      contains: "postgres:16-alpine"
  key_links:
    - from: "src/thinktank/api/main.py"
      to: "src/thinktank/database.py"
      via: "lifespan imports engine, verifies DB on startup"
      pattern: "engine"
    - from: "src/thinktank/api/health.py"
      to: "src/thinktank/database.py"
      via: "get_session dependency injection"
      pattern: "get_session"
    - from: "tests/integration/test_health.py"
      to: "src/thinktank/api/main.py"
      via: "httpx AsyncClient with app"
      pattern: "AsyncClient.*app"
---

<objective>
Create the ThinkTank project scaffold: pyproject.toml with all dependencies, FastAPI application with async lifespan, health endpoint, database connection layer, Docker Compose for dev and test PostgreSQL, and the full development toolchain (uv, ruff, mypy, pre-commit, pytest).

Purpose: Establish the runnable project skeleton that all subsequent plans build on. After this plan, `uv sync` works, the FastAPI app starts, and the test infrastructure is ready.
Output: A bootable FastAPI application with health endpoint, test infrastructure, and Docker Compose for PostgreSQL.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-layer/01-RESEARCH.md
@STANDARDS.md
@ThinkTank_Specification.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold with pyproject.toml, dependencies, and toolchain</name>
  <files>
    pyproject.toml
    .python-version
    .gitignore
    .env.example
    .pre-commit-config.yaml
    src/thinktank/__init__.py
    src/thinktank/worker/__init__.py
    tests/__init__.py
    tests/unit/__init__.py
    tests/integration/__init__.py
    docker-compose.yml
    docker-compose.test.yml
  </files>
  <behavior>
    - uv sync --frozen installs all dependencies (fastapi, uvicorn[standard], pydantic, pydantic-settings, sqlalchemy[asyncio], asyncpg, alembic, httpx, structlog)
    - uv sync --group dev installs dev dependencies (pytest, pytest-asyncio, ruff, mypy, pre-commit)
    - ruff check src/ passes with zero errors on the empty package
    - docker compose -f docker-compose.yml config validates without error
    - docker compose -f docker-compose.test.yml config validates without error
  </behavior>
  <action>
    Create the project from scratch:

    1. Create `pyproject.toml` following the exact configuration from 01-RESEARCH.md:
       - name = "thinktank", version = "0.1.0", requires-python = ">=3.12"
       - All production dependencies with pinned minimum versions from research
       - dependency-groups: dev (includes test + lint + pre-commit), test (pytest, pytest-asyncio), lint (ruff, mypy)
       - [tool.pytest.ini_options]: testpaths = ["tests"], asyncio_mode = "auto", asyncio_default_test_loop_scope = "session"
       - [tool.ruff]: target-version = "py312", line-length = 120, src = ["src"], select = ["E", "F", "I", "N", "W", "UP", "B", "A", "SIM", "TCH"]
       - [tool.ruff.format]: quote-style = "double"
       - [tool.mypy]: python_version = "3.12", warn_return_any = true, warn_unused_configs = true, disallow_untyped_defs = true, plugins = ["pydantic.mypy"]
       - [[tool.mypy.overrides]]: module = "tests.*", disallow_untyped_defs = false

    2. Create `.python-version` with content `3.12`

    3. Create `.gitignore` covering: __pycache__, *.pyc, .venv/, .env, *.egg-info, dist/, build/, .mypy_cache/, .pytest_cache/, .ruff_cache/, db data volumes, IDE files

    4. Create `.env.example` with all env vars from spec Section 10.2 as placeholders:
       DATABASE_URL=postgresql+asyncpg://thinktank:thinktank@localhost:5432/thinktank
       (and TEST_DATABASE_URL, ANTHROPIC_API_KEY, etc. with placeholder values)

    5. Create `.pre-commit-config.yaml` with ruff (lint + format) and mypy hooks

    6. Create `docker-compose.yml` with PostgreSQL 16-alpine service:
       - User: thinktank, password: thinktank, db: thinktank
       - Port 5432:5432
       - Volume pgdata for persistence
       - Healthcheck: pg_isready -U thinktank, interval 5s, timeout 5s, retries 5

    7. Create `docker-compose.test.yml` with PostgreSQL 16-alpine service:
       - User: thinktank_test, password: thinktank_test, db: thinktank_test
       - Port 5433:5432
       - Healthcheck: pg_isready -U thinktank_test, interval 2s, timeout 2s, retries 5
       - tmpfs: /var/lib/postgresql/data (RAM-backed for speed)

    8. Create empty __init__.py files for: src/thinktank/, src/thinktank/worker/, tests/, tests/unit/, tests/integration/

    9. Run `uv sync` to install all dependencies and generate uv.lock

    10. Run `uv run ruff check src/` to verify linting passes
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/luna/ThinkTank && uv sync && uv run ruff check src/ && uv run python -c "import fastapi; import sqlalchemy; import structlog; print('imports OK')"</automated>
  </verify>
  <done>pyproject.toml exists with all dependencies. uv sync succeeds. ruff check passes. Docker Compose configs validate. All package __init__.py files exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FastAPI application with database layer and health endpoint</name>
  <files>
    src/thinktank/database.py
    src/thinktank/api/__init__.py
    src/thinktank/api/main.py
    src/thinktank/api/health.py
    src/thinktank/api/dependencies.py
    tests/conftest.py
    tests/integration/test_health.py
  </files>
  <behavior>
    - GET /health returns 200 with {"status": "healthy", "service": "thinktank-api"} when DB connected
    - GET /health returns 503 with {"status": "unhealthy", "service": "thinktank-api"} when DB unreachable
    - FastAPI app has CORS middleware configured
    - Lifespan verifies DB connection on startup and disposes engine on shutdown
    - Session dependency provides AsyncSession with expire_on_commit=False
  </behavior>
  <action>
    1. Create `src/thinktank/database.py`:
       - `create_async_engine` with configurable DATABASE_URL (default from env or fallback)
       - `async_session_factory` via `async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)` -- CRITICAL: expire_on_commit=False for async
       - Pool config: pool_size=10, max_overflow=5, pool_pre_ping=True
       - Export: engine, async_session_factory

    2. Create `src/thinktank/api/dependencies.py`:
       - `get_session` async generator yielding AsyncSession from factory, auto-closing after use
       - This is the FastAPI dependency for DB access

    3. Create `src/thinktank/api/health.py`:
       - APIRouter with `GET /health`
       - Injects session via `Depends(get_session)`
       - Executes `SELECT 1` to verify DB connectivity
       - Returns 200 `{"status": "healthy", "service": "thinktank-api"}` on success
       - Returns 503 `{"status": "unhealthy", "service": "thinktank-api"}` on exception
       - Use `JSONResponse` for the 503 case with explicit status_code

    4. Create `src/thinktank/api/main.py`:
       - `@asynccontextmanager` lifespan function that:
         - On startup: verifies DB with `SELECT 1` via engine
         - On shutdown: disposes engine
       - `app = FastAPI(title="ThinkTank", version="0.1.0", lifespan=lifespan)`
       - `CORSMiddleware` with configurable origins (default ["*"] for dev)
       - Include health router
       - Create `src/thinktank/api/__init__.py` (empty)

    5. Create `tests/conftest.py`:
       - TEST_DATABASE_URL = env var or default "postgresql+asyncpg://thinktank_test:thinktank_test@localhost:5433/thinktank_test"
       - Session-scoped async `engine` fixture: creates async engine, yields, disposes
       - Per-test `session` fixture: connects, begins transaction, creates AsyncSession, yields, rolls back (using savepoint pattern from research)
       - `client` fixture: httpx AsyncClient with app for integration tests
       - Import Base from models once it exists; for now create a minimal Base for health tests

    6. Create `tests/integration/test_health.py`:
       - `test_health_returns_200_when_db_connected`: GET /health -> 200, body has status=healthy
       - `test_health_response_includes_service_name`: verify "service": "thinktank-api" in response
       - Tests use the httpx AsyncClient fixture

    Write tests first (RED), then implement (GREEN), then verify.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/luna/ThinkTank && docker compose -f docker-compose.test.yml up -d --wait && uv run pytest tests/integration/test_health.py -x --tb=short && docker compose -f docker-compose.test.yml down</automated>
  </verify>
  <done>FastAPI app starts and connects to PostgreSQL. Health endpoint returns 200 when DB is reachable. Integration tests pass against real PostgreSQL via Docker Compose.</done>
</task>

</tasks>

<verification>
1. `uv sync` completes without errors
2. `uv run ruff check src/` passes
3. `docker compose -f docker-compose.test.yml up -d --wait` starts PostgreSQL
4. `uv run pytest tests/integration/test_health.py -x` passes
5. `uv run python -c "from src.thinktank.api.main import app; print(app.title)"` prints "ThinkTank"
</verification>

<success_criteria>
- pyproject.toml contains all production and dev dependencies with correct versions
- FastAPI application boots with async lifespan, CORS, and health endpoint
- Health endpoint returns 200/503 based on DB connectivity
- Docker Compose provides PostgreSQL 16 for both dev (5432) and test (5433)
- Test infrastructure (conftest.py with engine/session/client fixtures) is ready
- ruff and mypy are configured and pass on the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layer/01-01-SUMMARY.md`
</output>
