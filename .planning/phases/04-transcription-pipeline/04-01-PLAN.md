---
phase: 04-transcription-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/thinktank/transcription/__init__.py
  - src/thinktank/transcription/captions.py
  - src/thinktank/transcription/existing.py
  - src/thinktank/transcription/audio.py
  - src/thinktank/transcription/gpu_client.py
  - src/thinktank/scaling/__init__.py
  - src/thinktank/scaling/railway.py
  - tests/unit/test_captions.py
  - tests/unit/test_existing_transcript.py
  - tests/unit/test_audio.py
  - tests/unit/test_gpu_client.py
  - tests/unit/test_railway_client.py
autonomous: true
requirements:
  - TRANS-01
  - TRANS-03
  - TRANS-05

must_haves:
  truths:
    - "YouTube caption extraction returns plain text or None when captions unavailable"
    - "Captions with fewer than 100 words are rejected (returns None)"
    - "Existing transcript fetch returns text from a URL derived from content URL + pattern"
    - "Audio download via yt-dlp produces a file at the expected path"
    - "ffmpeg converts audio to 16kHz mono WAV without blocking the async loop"
    - "Temp files are deleted on both success and failure paths"
    - "GPU client sends WAV to GPU service and returns transcript text"
    - "Railway scaling client calls correct GraphQL mutation"
  artifacts:
    - path: "src/thinktank/transcription/captions.py"
      provides: "Pass 1 YouTube caption extraction"
      exports: ["extract_youtube_captions"]
    - path: "src/thinktank/transcription/existing.py"
      provides: "Pass 2 existing transcript fetch"
      exports: ["fetch_existing_transcript"]
    - path: "src/thinktank/transcription/audio.py"
      provides: "Audio download and ffmpeg conversion with cleanup"
      exports: ["download_audio", "convert_to_wav"]
    - path: "src/thinktank/transcription/gpu_client.py"
      provides: "HTTP client for GPU worker service"
      exports: ["transcribe_via_gpu"]
    - path: "src/thinktank/scaling/railway.py"
      provides: "Railway GraphQL client for GPU scaling"
      exports: ["scale_gpu_service", "get_gpu_replica_count"]
  key_links:
    - from: "src/thinktank/transcription/audio.py"
      to: "yt-dlp"
      via: "YoutubeDL Python API for audio download"
      pattern: "from yt_dlp import YoutubeDL"
    - from: "src/thinktank/transcription/audio.py"
      to: "ffmpeg"
      via: "asyncio.create_subprocess_exec for 16kHz conversion"
      pattern: "asyncio.create_subprocess_exec.*ffmpeg"
    - from: "src/thinktank/transcription/gpu_client.py"
      to: "httpx"
      via: "Multipart POST to GPU worker /transcribe endpoint"
      pattern: "httpx.*AsyncClient.*post.*transcribe"
    - from: "src/thinktank/scaling/railway.py"
      to: "Railway GraphQL API"
      via: "httpx POST to backboard.railway.com"
      pattern: "backboard.railway.com/graphql"
---

<objective>
Build all transcription building-block modules: YouTube caption extraction (Pass 1), existing transcript fetch (Pass 2), audio download/conversion with temp file lifecycle (Pass 3 prep), GPU worker HTTP client, and Railway API scaling client. Add yt-dlp and webvtt-py dependencies. Comprehensive unit tests for every module.

Purpose: These are the pure-logic and I/O-wrapper modules that the `process_content` handler (Plan 02) will orchestrate. Building them first with full test coverage means Plan 02 can focus purely on orchestration wiring.

Output: 2 new packages (transcription/, scaling/), 5 module files, 5 unit test files, updated pyproject.toml.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transcription-pipeline/04-RESEARCH.md
@STANDARDS.md

<interfaces>
<!-- Key types and contracts from prior phases that this plan consumes. -->

From src/thinktank/models/content.py:
```python
class Content(Base):
    id: Mapped[uuid_pk]
    source_id: Mapped[uuid.UUID]
    url: Mapped[str]
    canonical_url: Mapped[str]
    body_text: Mapped[Optional[str]]
    word_count: Mapped[Optional[int]]
    duration_seconds: Mapped[Optional[int]]
    transcription_method: Mapped[Optional[str]]  # "youtube_captions", "existing_transcript", "parakeet"
    status: Mapped[str]  # "pending", "processing", "done", "error", "skipped"
    processed_at: Mapped[Optional[datetime]]
```

From src/thinktank/models/source.py:
```python
class Source(Base):
    source_type: Mapped[str]  # "podcast_rss", "youtube_channel", etc.
    config: Mapped[dict]  # JSONB -- may contain "transcript_url_pattern"
```

From src/thinktank/queue/errors.py:
```python
class ErrorCategory(StrEnum):
    TRANSCRIPTION_FAILED = "transcription_failed"
    AUDIO_DOWNLOAD_FAILED = "audio_download_failed"
    AUDIO_CONVERSION_FAILED = "audio_conversion_failed"
```

From pyproject.toml (current dependencies -- yt-dlp and webvtt-py NOT yet present):
```toml
dependencies = [
    "fastapi>=0.135.1", "uvicorn[standard]>=0.41.0", "pydantic>=2.12.5",
    "pydantic-settings>=2.13.1", "sqlalchemy[asyncio]>=2.0.46", "asyncpg",
    "alembic>=1.17.2", "httpx>=0.28.1", "structlog>=25.5.0", "feedparser>=6.0.12",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies + create transcription modules (captions, existing, audio) with unit tests</name>
  <files>
    pyproject.toml,
    src/thinktank/transcription/__init__.py,
    src/thinktank/transcription/captions.py,
    src/thinktank/transcription/existing.py,
    src/thinktank/transcription/audio.py,
    tests/unit/test_captions.py,
    tests/unit/test_existing_transcript.py,
    tests/unit/test_audio.py
  </files>
  <behavior>
    test_captions.py:
    - test_extract_captions_success: Mock yt-dlp extract_info to return requested_subtitles with en sub URL + mock httpx to return VTT content -> returns plain text with >= 100 words
    - test_extract_captions_no_subs: Mock yt-dlp extract_info returning empty requested_subtitles -> returns None
    - test_extract_captions_too_short: Mock returns VTT with only 50 words -> returns None (< 100 word threshold)
    - test_extract_captions_exception_returns_none: Mock yt-dlp raising an exception -> returns None (fail-safe, fall through to next pass)
    - test_vtt_parsing_strips_timing: VTT content with timestamps and duplicate lines -> plain text with duplicates removed

    test_existing_transcript.py:
    - test_fetch_existing_success: Mock httpx GET returning HTML with transcript text -> extracted plain text returned
    - test_fetch_existing_no_pattern: No transcript_url_pattern -> returns None immediately
    - test_fetch_existing_404: Mock httpx returning 404 -> returns None
    - test_fetch_existing_timeout: Mock httpx raising TimeoutError -> returns None

    test_audio.py:
    - test_download_audio_success: Mock yt-dlp to create a file at expected path -> returns path string
    - test_download_audio_failure: Mock yt-dlp raising DownloadError -> raises RuntimeError with AUDIO_DOWNLOAD_FAILED context
    - test_convert_to_wav_success: Mock asyncio.create_subprocess_exec with returncode=0 -> returns .wav path
    - test_convert_to_wav_failure: Mock subprocess with returncode=1 -> raises RuntimeError
    - test_convert_to_wav_timeout: Mock asyncio.wait_for raising TimeoutError -> raises RuntimeError
    - test_cleanup_on_success: After successful transcribe_via_gpu call, verify temp files are deleted
    - test_cleanup_on_failure: After failed transcribe_via_gpu call, verify temp files are still deleted (finally block)
  </behavior>
  <action>
    1. Add dependencies to pyproject.toml:
       - Add `"yt-dlp==2025.12.08"` and `"webvtt-py>=0.5.1"` to the `dependencies` list.
       - Run `uv sync` to install.

    2. Create `src/thinktank/transcription/__init__.py` (empty).

    3. Create `src/thinktank/transcription/captions.py`:
       - `def extract_youtube_captions(video_url: str) -> str | None`
       - Uses yt-dlp Python API with opts: `writeautomaticsub=True, writesubtitles=True, subtitleslangs=["en"], skip_download=True, quiet=True, no_warnings=True`
       - Calls `ydl.extract_info(video_url, download=False)`
       - Gets `requested_subtitles.en.url`, fetches VTT content via httpx.get (sync, since yt-dlp is sync)
       - Parses VTT using webvtt-py: iterate captions, collect unique text lines (skip duplicates from overlapping cues)
       - If `len(text.split()) >= 100`, return text. Else return None.
       - Entire function wrapped in try/except -> return None on any exception (fail-safe per spec 7.1).
       - Use structlog for logging.

    4. Create `src/thinktank/transcription/existing.py`:
       - `async def fetch_existing_transcript(content_url: str, transcript_url_pattern: str) -> str | None`
       - Derives transcript URL from content_url using pattern substitution (e.g., pattern = "https://show.com/transcripts/{slug}" where slug is derived from content URL path).
       - Simple approach for v1: use `re.sub` or string replacement to transform content URL to transcript URL using the pattern. The pattern is a format string where `{episode_id}` or `{slug}` is replaced with a part extracted from the content URL.
       - Fetches via `httpx.AsyncClient.get()` with 30s timeout.
       - If 200 response: strip HTML tags (basic regex `re.sub(r'<[^>]+>', '', text)`) to extract plain text, return if non-empty.
       - On 4xx/5xx or timeout: return None (transcript doesn't exist).
       - Wrap in try/except -> return None.

    5. Create `src/thinktank/transcription/audio.py`:
       - `def download_audio(url: str, tmp_dir: str) -> str`: Sync function using yt-dlp Python API. Sets `format="bestaudio/best"`, `outtmpl=os.path.join(tmp_dir, f"audio_{uuid4().hex[:12]}.%(ext)s")`, postprocessors=[{"key": "FFmpegExtractAudio", "preferredcodec": "wav"}]. After download, scans for the actual output file (yt-dlp may change extension). Returns path. Raises RuntimeError on failure.
       - `async def convert_to_wav(input_path: str, tmp_dir: str) -> str`: Uses `asyncio.create_subprocess_exec("ffmpeg", "-i", input_path, "-ar", "16000", "-ac", "1", "-acodec", "pcm_s16le", "-y", output_path, ...)`. Uses `asyncio.wait_for(proc.communicate(), timeout=300)`. Returns output_path. Raises RuntimeError on non-zero return code or timeout.
       - `async def transcribe_via_gpu(content_url: str, tmp_dir: str, gpu_client_fn) -> str`: Orchestrates download + convert + GPU call with guaranteed cleanup in `finally` block. Takes a callable `gpu_client_fn` (dependency injection for testability) that accepts a WAV path and returns text. Deletes all temp files (audio_path, wav_path) in finally. Returns transcript text or raises.
       - Use `os.environ.get("AUDIO_TMP_DIR", tempfile.gettempdir())` for default tmp_dir.
       - Structlog throughout.

    6. Write all three unit test files. All external I/O (yt-dlp, httpx, subprocess) mocked via `unittest.mock.patch` and `AsyncMock`. Tests verify behavior, not implementation details. The audio cleanup tests create real temp files and verify they're deleted.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/luna/ThinkTank && uv run pytest tests/unit/test_captions.py tests/unit/test_existing_transcript.py tests/unit/test_audio.py -x -q</automated>
  </verify>
  <done>
    All three transcription modules exist with full unit test coverage. yt-dlp and webvtt-py installed. Caption extraction handles success, no-subs, too-short, and exception cases. Existing transcript fetch handles success, no-pattern, 404, and timeout. Audio module handles download, conversion, and guaranteed cleanup on both success and failure paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GPU client and Railway scaling client with unit tests</name>
  <files>
    src/thinktank/transcription/gpu_client.py,
    src/thinktank/scaling/__init__.py,
    src/thinktank/scaling/railway.py,
    tests/unit/test_gpu_client.py,
    tests/unit/test_railway_client.py
  </files>
  <behavior>
    test_gpu_client.py:
    - test_transcribe_success: Mock httpx POST to /transcribe returning {"text": "hello world..."} -> returns transcript text
    - test_transcribe_timeout: Mock httpx raising timeout -> raises RuntimeError
    - test_transcribe_gpu_error: Mock httpx returning 500 -> raises RuntimeError
    - test_transcribe_sends_multipart: Verify the mock received a multipart file upload with the WAV data
    - test_long_audio_chunking: Mock a file > 60min duration -> verify it's split into 45-min segments, each sent separately, results concatenated

    test_railway_client.py:
    - test_scale_up_calls_mutation: Mock httpx POST -> verify GraphQL mutation sent with numReplicas=1
    - test_scale_down_calls_mutation: Mock httpx POST -> verify GraphQL mutation sent with numReplicas=0
    - test_scale_missing_config: Unset env vars -> returns False, logs warning
    - test_scale_api_error: Mock httpx returning error -> returns False
    - test_get_replica_count: Mock GraphQL query response -> returns correct count
    - test_manage_gpu_scaling_scale_up: Queue depth > threshold, replicas=0 -> calls scale_gpu_service(1)
    - test_manage_gpu_scaling_scale_down: Queue depth=0, idle elapsed > timeout -> calls scale_gpu_service(0)
    - test_manage_gpu_scaling_no_action: Queue depth=0 but idle time < timeout -> no scale call
  </behavior>
  <action>
    1. Create `src/thinktank/transcription/gpu_client.py`:
       - `async def send_to_gpu(wav_path: str, gpu_url: str | None = None) -> str`: Sends WAV file as multipart POST to `{gpu_url}/transcribe`. Default gpu_url from `os.environ.get("GPU_WORKER_URL", "http://worker-gpu.railway.internal:8000")`. Uses httpx.AsyncClient with timeout=600s (10min for long audio). Returns response JSON `text` field. Raises RuntimeError on error/timeout.
       - `async def transcribe_with_chunking(wav_path: str, duration_seconds: int | None, gpu_url: str | None = None) -> str`: If duration_seconds and duration_seconds > 3600 (60 min), splits WAV into 45-min segments using ffmpeg subprocess (same async pattern as convert_to_wav), sends each chunk to GPU, concatenates results with space. Otherwise sends full file. Cleans up chunk files in finally block.
       - Structlog throughout.

    2. Create `src/thinktank/scaling/__init__.py` (empty).

    3. Create `src/thinktank/scaling/railway.py`:
       - Constants: `RAILWAY_API_URL = "https://backboard.railway.com/graphql/v2"`
       - `async def scale_gpu_service(replicas: int) -> bool`: Reads `RAILWAY_API_KEY`, `RAILWAY_GPU_SERVICE_ID`, `RAILWAY_ENVIRONMENT_ID` from env. If any missing, log warning and return False. Sends `serviceInstanceUpdate` mutation via httpx POST. Returns True on success, False on error. Logs result.
       - `async def get_gpu_replica_count() -> int | None`: Sends `serviceInstance` query to get current numReplicas. Returns count or None on error.
       - `async def manage_gpu_scaling(session: AsyncSession, gpu_idle_since: datetime | None) -> tuple[bool, datetime | None]`: Reads `gpu_queue_threshold` and `gpu_idle_minutes_before_shutdown` from system_config via `get_config_value`. Calls `get_queue_depth(session, "process_content")`. Logic:
         - If depth > threshold AND current replicas == 0 -> scale_gpu_service(1), return (True, None)
         - If depth == 0 AND gpu_idle_since is None -> return (False, _now()) to start idle timer
         - If depth == 0 AND gpu_idle_since is not None AND elapsed > idle_minutes -> scale_gpu_service(0), return (True, None)
         - If depth > 0 -> return (False, None) to reset idle timer
         - Returns (scaled: bool, new_idle_since: datetime | None)
       - Import `get_queue_depth` from `src.thinktank.queue.backpressure` and `get_config_value` from `src.thinktank.ingestion.config_reader`.
       - Structlog throughout.

    4. Write unit test files. Mock all httpx calls, env vars (via monkeypatch), and database calls (mock session + get_queue_depth + get_config_value).
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/luna/ThinkTank && uv run pytest tests/unit/test_gpu_client.py tests/unit/test_railway_client.py -x -q</automated>
  </verify>
  <done>
    GPU client sends multipart WAV to GPU service, handles chunking for >60min audio. Railway client manages GPU scaling via GraphQL API. manage_gpu_scaling implements scale-up on queue depth threshold and scale-down on idle timeout. All unit tests pass with mocked I/O.
  </done>
</task>

</tasks>

<verification>
All unit tests for transcription and scaling modules:
- `uv run pytest tests/unit/test_captions.py tests/unit/test_existing_transcript.py tests/unit/test_audio.py tests/unit/test_gpu_client.py tests/unit/test_railway_client.py -x -q`
- Full suite regression: `uv run pytest tests/ -x -q`
- Import check: `python -c "from src.thinktank.transcription.captions import extract_youtube_captions; from src.thinktank.transcription.existing import fetch_existing_transcript; from src.thinktank.transcription.audio import download_audio, convert_to_wav; from src.thinktank.transcription.gpu_client import send_to_gpu; from src.thinktank.scaling.railway import scale_gpu_service, manage_gpu_scaling; print('All imports OK')"`
</verification>

<success_criteria>
1. yt-dlp==2025.12.08 and webvtt-py>=0.5.1 added to pyproject.toml and installed
2. Caption extraction returns text for valid YouTube subs, None for missing/short/error cases
3. Existing transcript fetch derives URL from pattern, returns text or None on failure
4. Audio download uses yt-dlp Python API, conversion uses async ffmpeg subprocess with 5-min timeout
5. Temp file cleanup guaranteed via finally blocks on both success and failure
6. GPU client sends multipart WAV, handles chunking for long audio (>60min -> 45-min segments)
7. Railway client sends correct GraphQL mutations for scale up/down
8. manage_gpu_scaling implements threshold-based scale-up and idle-timeout scale-down
9. All unit tests pass, full suite green: `uv run pytest tests/ -x -q`
</success_criteria>

<output>
After completion, create `.planning/phases/04-transcription-pipeline/04-01-SUMMARY.md`
</output>
