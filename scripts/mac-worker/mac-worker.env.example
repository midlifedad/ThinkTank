# Environment for the dockerized Mac pull-worker.
# Copy to mac-worker.env (gitignored) and fill in. NEVER commit the real file.

# Railway Postgres EXTERNAL connection string -- issued by deploy-ops.
# Must use the asyncpg scheme:
DATABASE_URL=postgresql+asyncpg://user:password@host.proxy.rlwy.net:12345/railway
